<div class="containe my-5">
  <h1 class="text-center"><%= link_to "ここは管理者専用ページです。", admin_session_path %></h1>
</div>

注文履歴一覧
<table>
  <tr>
    <td>購入日時</td>
    <td>購入者</td>
    <td>注文個数</td>
    <td>注文ステータス</td>
  </tr>
    <% @orders.each do |order| %>
      <tr>
          <td>
            <%= link_to  admin_order_item_path(order.id) do %>
              <%=  l order.created_at %>
            <% end %>
          </td>
          <td><%= order.customer.first_name%><%= order.customer.last_name %></td>
          <td><%= order.order_items.sum(:number) %></td>
          <td><%= order.status_i18n %></td>
      </tr>
    <% end %>
</table>