<div class="container my-5" >
  <div class="row">
    <div class="col-3 mb-3 offset-1">
      <h3><strong class="border-bottom border-dark">会員一覧</strong></h3>
    </div>

      <div class="container mb-5">
        <div class="row">
          <div class="col-10 mx-auto">
            <table class="table table-hover text-center contents-area">
              <thead>
                <tr>
                  <th>会員ID</th>
                  <th>氏名</th>
                  <th>メールアドレス</th>
                  <th>ステータス</th>
                </tr>
              </thead>
              <% @customers.each do |customer| %>
              <tbody>
                <tr>
                <td class="py-4"><%= customer.id %></td>
                <td class="py-4">
                  <div class="link-secondary">
                    <%= link_to admin_customer_path(customer.id) do %>
                      <%= customer.last_name %><%= customer.first_name %>
                    <% end %>
                  </div>
                </td>
                <td class="py-4"><%= customer.email %></td>
                <td class="py-4">
                  <% if customer.is_deleted == false %>
                    <strong class="text-success">有効</strong>
                  <% elsif %>
                    <strong class="text-secondary">退会</stropng>
                  <% end %>
                </td>
                </tr>
              </tbody>
              <% end %>
            </table>
            <div class="pagination justify-content-center mt-3">
              <%= paginate @customers, theme: 'bootstrap-5' %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>