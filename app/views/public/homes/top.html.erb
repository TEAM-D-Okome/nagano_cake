<div class="container">
  <%= flash[:notice] %>
  <div class="row mt-5">
    <!--ジャンル検索フォーム用のcol-->
    <div class="col-3 mr-3">
      <%= render 'genrebar', genres: @genres %>
    </div>

    <div class="col-8">
      <div class="card">
        <%= image_tag 'top_image.jpg' %>
        <div class="card-img-overlay d-flex align-items-end">
          <p>
            ようこそ、ながのCAKEへ！</br>
            このサイトは、ケーキ販売のECサイトになります。</br>
            会員でない方も商品の閲覧はできますが、</br>
            購入には会員登録が必要になります。
          </p>
        </div>
      </div>

      <h3 class="mt-2 mb-2"><strong>新着商品</strong></h3>
      <div class="d-flex flex-row">
        <% @items.first(4).each do |item| %>
        <div class="mr-2">
          <%= link_to item_path(item) do %>
            <div><%= image_tag item.get_image(150,100) %></div></br>
            <p><%= item.name %></p>
            <% if item.is_sale == true %>
              <p><%= item.add_tax_tax_out_price %>円(税抜き)</p>
            <% else %>
              <p class="text-danger">販売停止中</p>
            <% end %>
          <% end %>
        </div>
        <% end %>
      </div>
      <div class="mt-2 mb-3 text-right"><%= link_to "全ての商品を見る", items_path %></div>
    </div>
  </div>
</div>